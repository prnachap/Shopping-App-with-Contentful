import React from "react";
import { ReactComponent as Loader } from "../../assets/loader.svg";

const Spinner = () => {
  return (
    <div>
      <Loader />
    </div>
  );
};

export default Spinner;
